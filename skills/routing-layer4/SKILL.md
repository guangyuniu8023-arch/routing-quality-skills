---
name: routing-layer4
description: Use when all routing quality layers are complete and full regression testing across all case types is needed
---

# Routing Layer 4: Integration

Read and follow `${CLAUDE_SKILL_DIR}/../../docs/layer4-integration.md`.

Requires `.routing-quality/snapshots/v1-golden/`, `v2-ambiguity/`, and `v3-boundary/` from prior layers.

References in `${CLAUDE_SKILL_DIR}/../../docs/references/` are loaded on demand during execution.
