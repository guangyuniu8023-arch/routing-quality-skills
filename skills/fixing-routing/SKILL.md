---
name: fixing-routing
description: Use when a single routing bug is found from screenshots, logs, test failures, or user reports and needs quick diagnosis and fix
---

# Fixing Routing

Read and follow `${CLAUDE_SKILL_DIR}/../../docs/fixing-routing.md`.

If `.routing-quality/config.md` does not exist, run `${CLAUDE_SKILL_DIR}/../../docs/phase0-discovery.md` first.

References in `${CLAUDE_SKILL_DIR}/../../docs/references/` are loaded on demand during execution.
