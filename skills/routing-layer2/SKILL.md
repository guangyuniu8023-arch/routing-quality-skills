---
name: routing-layer2
description: Use when Layer 1 golden cases are complete and ambiguous routing inputs need systematic identification and resolution
---

# Routing Layer 2: Ambiguity Cases

Read and follow `${CLAUDE_SKILL_DIR}/../../docs/layer2-ambiguity-cases.md`.

Requires `.routing-quality/snapshots/v1-golden/` from Layer 1.

References in `${CLAUDE_SKILL_DIR}/../../docs/references/` are loaded on demand during execution.
