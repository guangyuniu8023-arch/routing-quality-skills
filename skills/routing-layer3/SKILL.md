---
name: routing-layer3
description: Use when Layer 2 ambiguity cases are complete and competing skill pairs need boundary arbitration and fallback rules
---

# Routing Layer 3: Boundary Cases

Read and follow `${CLAUDE_SKILL_DIR}/../../docs/layer3-boundary-cases.md`.

Requires `.routing-quality/snapshots/v1-golden/` and `.routing-quality/snapshots/v2-ambiguity/` from prior layers.

References in `${CLAUDE_SKILL_DIR}/../../docs/references/` are loaded on demand during execution.
