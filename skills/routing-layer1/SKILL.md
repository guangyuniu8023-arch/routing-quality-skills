---
name: routing-layer1
description: Use when starting systematic routing quality improvement, establishing golden cases for each skill's core scenarios
---

# Routing Layer 1: Golden Cases

Read and follow `${CLAUDE_SKILL_DIR}/../../docs/layer1-golden-cases.md`.

If `.routing-quality/config.md` does not exist, run `${CLAUDE_SKILL_DIR}/../../docs/phase0-discovery.md` first.

References in `${CLAUDE_SKILL_DIR}/../../docs/references/` are loaded on demand during execution.
